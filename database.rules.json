{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "buckets": {
      ".indexOn": ["owner"],
      "$bucket_id": {
        "transactions": {
          ".indexOn": ["date"]
        }
      }
    }
  }
}
