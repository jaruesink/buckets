{
  "rules": {
    "buckets": {
      ".read": "(auth != null)",
      ".write": "(auth != null)",
      ".indexOn": ["owner"],
      "$bucket_id": {
        "transactions": {
          ".indexOn": ["date"]
        }
      }
    }
  }
}
