{
  "rules": {
    ".read": true,
    ".write": true,
    "buckets": {
      ".indexOn": ["owner"],
      "$bucket_id": {
        "transactions": {
          ".indexOn": ["date"]
        }
      }
    }
  }
}
